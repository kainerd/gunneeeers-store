# Deny ALL direct HTTP access to includes (helpers, layout partials).
# They must only be pulled in via require/include from public PHP scripts.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
