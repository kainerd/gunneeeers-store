# Deny ALL direct HTTP access to this directory.
# Config holds DB credentials — never serve these files as URLs.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
